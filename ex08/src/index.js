function main(numOne, numTwo) {
    var numOne;
    var numTwo;
    var sum;
    numOne = parseInt(prompt('Enter the first number '));
    numTwo = parseInt(prompt('Enter the second number '));
    sum = numOne+numTwo;   
    return sum;
    }
    console.log(main(numOne, numTwo, sum));
    module.exports = main;
